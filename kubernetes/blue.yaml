/*apiVersion: apps/v1
kind: Deployment
metadata:
  name: stockmanager-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stockmanager
      version: blue
  template:
    metadata:
      labels:
        app: stockmanager
        version: blue
    spec:
      imagePullSecrets:
        - name: ecr-secret
      containers:
        - name: stockmanager
          image: 493800112687.dkr.ecr.eu-north-1.amazonaws.com/shopfront:14
          ports:
            - containerPort: 8010
*/


metadata:
  name: stockmanager-blue
spec:
  selector:
    matchLabels:
      app: stockmanager
      version: blue
