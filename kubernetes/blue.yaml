apiVersion: apps/v1
kind: Deployment
metadata:
  name: stockmanager-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stockmanager
      version: blue
  template:
    metadata:
      labels:
        app: stockmanager
        version: blue
    spec:
      imagePullSecrets:
      - name: ecr-secret
      containers:
      - name: stockmanager
        image: IMAGE_PLACEHOLDER
        ports:
        - containerPort: 8030
