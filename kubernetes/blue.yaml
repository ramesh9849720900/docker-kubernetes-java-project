
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stockmanager-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stockmanager
      version: blue
  template:
    metadata:
      labels:
        app: stockmanager
        version: blue
    spec:
      containers:
      - name: stockmanager
        image: shopfront:v1
        ports:
        - containerPort: 8030
