apiVersion: apps/v1
kind: Deployment
metadata:
  name: stockmanager-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stockmanager
      version: green
  template:
    metadata:
      labels:
        app: stockmanager
        version: green
    spec:
      imagePullSecrets:
      - name: ecr-secret
      containers:
      - name: stockmanager
        image: IMAGE_PLACEHOLDER
        ports:
        - containerPort: 8080
