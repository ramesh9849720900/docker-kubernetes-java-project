
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stockmanager-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stockmanager
      version: green
  template:
    metadata:
      labels:
        app: stockmanager
        version: green
    spec:
      containers:
      - name: stockmanager
        image: shopfront:v1
        ports:
        - containerPort: 8030
